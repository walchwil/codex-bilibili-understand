[project]
name = "codex-bilibili-understand-runtime"
version = "0.5.0"
description = "Isolated Python runtime for the Codex Bilibili Understand plugin"
requires-python = ">=3.10"
dependencies = [
  "yt-dlp>=2026.8.19",
  "faster-whisper>=1.2.1,<2",
]

[tool.uv]
package = false
