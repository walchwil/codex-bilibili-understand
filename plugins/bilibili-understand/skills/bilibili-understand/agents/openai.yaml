interface:
  display_name: "Bilibili Understand"
  short_description: "Use captions or hot-transcribe only the needed range"
  default_prompt: "Use $bilibili-understand to prefer normalized captions, otherwise transcribe only the requested Bilibili time range when possible, use full context only when necessary, and cite second-level timestamps."
