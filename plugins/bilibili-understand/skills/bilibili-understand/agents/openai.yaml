interface:
  display_name: "Bilibili Understand"
  short_description: "Hot-transcribe only the Bilibili range needed"
  default_prompt: "Use $bilibili-understand to transcribe only the requested Bilibili time range when possible, use full context only when necessary, and cite second-level timestamps."
